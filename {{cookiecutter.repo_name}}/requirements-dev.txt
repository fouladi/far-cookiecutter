# Dev dependencies are managed in pyproject.toml under [dependency-groups].
# Install them with:
#   uv sync --group dev
#
# If you are not using uv, you can install them manually:
#   pip install ruff ty pytest pytest-cov pytest-mock prek
